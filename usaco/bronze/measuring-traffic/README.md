## Measuing Traffic (USACO Bronze)

For this problem, I used two integer arrays of size `n` and a string array of size `n`. After analyzing the sample case, I created an algorithm to minimize the range of a and b through simulating all of the `n` road segments.