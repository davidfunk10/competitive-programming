# Increasing Array (CSES Intro)
Implementation problem where I compared the previous value to the current value and updated a counter variable named `moves` based on the difference between `prev` and `cur`.
